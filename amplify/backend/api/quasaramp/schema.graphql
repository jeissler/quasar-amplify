type Todo
@model
@auth(rules: [{ allow: owner }, { allow: public, operations: [read] }]) {
  id: ID!
  category: String!
  name: String!
  title: String
  status: String
  link: String
  description: String
}
